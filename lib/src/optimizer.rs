use super::stmt::*;

/// the optimizer takes the parsed AST and attempts
/// to apply some steps to improve code speed
/// and reduce code size

pub struct Optimizer;

impl Optimizer {
    pub fn optimize(_stmts: &mut Vec<Stmt>) {
    }
}
